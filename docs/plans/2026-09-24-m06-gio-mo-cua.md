# Kế hoạch 2026-09-24 — M-06 (= S6): đặt lịch phải nằm trong giờ mở cửa

**Lỗi:** [`../testing/reports/2026-09-19-ra-luong-P1-P7.md`](../testing/reports/2026-09-19-ra-luong-P1-P7.md) mục M-06.
`scheduling.dat_lich` **không kiểm giờ làm việc**. Đo trên giao diện 19/09: đặt được lịch **03:00**,
đặt được buổi **23:50 → 01:20 hôm sau**, và một dịch vụ 2.000 phút giữ nhân viên **hơn 33 giờ**.
Hằng `GIO_MO_CUA = 8` / `GIO_DONG_CUA = 18` có trong `scheduling.py` từ P3 nhưng **chỉ phần gợi ý
khung trống dùng tới**.

Đề bài nêu đích danh nghiệp vụ lịch hẹn ở mục 6 (*"KT2: … debug trùng lịch"*), nên đây là lỗi nặng
nhất trong 11 lỗi còn lại xét theo trọng tâm môn học.

## Bốn quyết định của người dùng (24/09)

1. **Cả buổi phải nằm trọn trong giờ mở cửa**: `bat_dau >= 08:00` **và** `ket_thuc <= 18:00`, cùng
   một ngày. Một luật diệt cả ba triệu chứng — 3 giờ sáng, lấn qua đêm, và buổi 33 giờ.
2. **Chặn dịch vụ dài hơn ngày làm việc ở CẢ HAI chỗ**: lúc tạo/sửa dịch vụ (600 phút) và kiểm lại
   lúc đặt lịch (dịch vụ cũ trong CSDL có thể đã vượt). Đúng bài học 4 — sửa cả lớp lỗi.
3. **Giữ hằng số trong code**, không đưa ra `.env`. CLAUDE.md mục 2: không thêm "cấu hình được" khi
   không ai yêu cầu.
4. **Không tính ngày nghỉ / cuối tuần** — ngoài phạm vi M-06.

## Hiện trạng dữ liệu (đo 24/09, `petcare.db` read-only)

9 lịch hẹn **đều trong 08:00–18:00 cùng ngày**; 5 dịch vụ dài nhất **90 phút**. Ba bản ghi rác
#14/#15/#16 của lượt rà 19/09 đã mất khi khôi phục CSDL ngày 20/09. **Không có dữ liệu cũ vi phạm,
nên không cần bước vá dữ liệu.**

## Các bước

- [x] 1. **Test đỏ trước** — `tests/unit/test_scheduling.py`: đặt lịch 07:59 bị từ chối · 08:00 được
      nhận (ca biên dưới) · buổi kết thúc đúng 18:00 được nhận · vượt 18:00 bị từ chối (ca biên trên) ·
      buổi lấn qua nửa đêm bị từ chối · **`doi_lich` cũng chịu đúng luật đó** (bài học 4) ·
      dịch vụ dài hơn 600 phút không đặt được dù bắt đầu lúc 08:00.
      → verify: chạy, đếm đúng số ca đỏ, và các ca biên "được nhận" phải **xanh ngay** (chúng mô tả
      hành vi hiện có, không được đỏ — nếu đỏ là test viết sai).
- [x] 2. **Test đỏ trước** — `tests/unit/test_catalog_service.py`: tạo dịch vụ 601 phút bị từ chối ·
      600 phút được nhận (ca biên) · sửa dịch vụ lên 601 phút bị từ chối.
      → verify: 2 ca đỏ, 1 ca xanh.
- [x] 3. **Cài đặt `scheduling.py`**: hằng `PHUT_LAM_VIEC_MOI_NGAY`, hàm `_kiem_gio_lam_viec()`,
      gọi trong `dat_lich` **và** `doi_lich`.
      → verify: bước 1 xanh hết.
- [x] 4. **Cài đặt `catalog.py`**: `_kiem_thoi_luong` chặn thêm cận trên.
      → verify: bước 2 xanh hết.
- [x] 5. **Đột biến có chủ đích** — ít nhất 3 lượt, mỗi lượt xác nhận đã vào file trước khi chạy:
      bỏ phép kiểm ở `dat_lich` · bỏ phép kiểm ở `doi_lich` (phải đỏ riêng, chứng minh không chỉ
      sửa một nửa lớp lỗi) · nới cận trên `catalog` lên 10.000 phút.
      → verify: mỗi lượt đúng nhóm ca tương ứng đỏ, hoàn nguyên xong xanh lại.
- [x] 6. **Soát cả lớp lỗi** (bài học 4): còn đường nào ghi `start_at`/`end_at` mà không qua hai hàm
      trên không? Gợi ý khung trống có còn khớp luật mới không? `seed.py` có sinh lịch vi phạm không?
      → verify: liệt kê từng chỗ đã soát trong báo cáo, không chỉ nói "đã soát".
- [x] 7. **Chạy toàn bộ suite**, ghi số thật.
      → verify: exit 0, không ca nào đỏ ngoài dự kiến.
- [x] 8. **Cập nhật tài liệu**: `test-cases.md` (TC-122), `roadmap.md` (M-06/S6 → đã sửa),
      báo cáo rà soát 19/09 (mục "Đã sửa"), `codebase-map.md`, log phiên.
      → verify: 50 phép canh kiến trúc xanh.
