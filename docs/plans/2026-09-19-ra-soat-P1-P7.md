# Rà soát toàn hệ thống P1 → P7 bằng trình duyệt và HTTP

> **Duyệt ngày:** 2026-09-19 (người dùng trả lời hai vòng câu hỏi) · **Trạng thái:** rà xong 19/09 — 3 cao · 8 trung bình ·
> 7 thấp, chờ người dùng chọn lỗi cần sửa. Báo cáo: [`../testing/reports/2026-09-19-ra-luong-P1-P7.md`](../testing/reports/2026-09-19-ra-luong-P1-P7.md)
>
> Mục tiêu: tìm lỗi của hệ thống sau P7 trước khi người dùng tick smoke. **Chỉ tìm và ghi lại**,
> chưa sửa gì — sửa lỗi nào do người dùng chọn sau khi đọc báo cáo, mỗi lỗi sửa có test đỏ-trước.

## Quyết định đã chốt

| # | Câu hỏi | Người dùng chọn |
|---|---|---|
| R1 | Phạm vi | Toàn hệ thống P1 → P7, theo 3 vai trò, kiểm luồng thông tin giữa các màn hình |
| R2 | Dữ liệu | `petcare.db` thật. **Sao lưu trước** (`petcare.backup-2026-09-19.db`). Dữ liệu rà mang tiền tố **"Rà 19/09"**; xóa/hủy chỉ làm trên dữ liệu rà, không đụng dữ liệu seed |
| R3 | AI | Gemini thật toàn bộ; cho xoay sang model dự phòng; **tối đa ~25 lượt** |
| R4 | Hướng phá | Phân quyền · dữ liệu biên và sai · bảo mật cơ bản · nhất quán số liệu |
| R5 | Khi có lỗi | Ghi lại hết kèm bằng chứng, xếp theo mức nghiêm trọng, **hỏi rồi mới sửa** |
| R6 | Đầu ra | Báo cáo `docs/testing/reports/2026-09-19-ra-luong-P1-P7.md` + ảnh chụp lỗi. Smoke **không tick** — người dùng tự bấm |
| R7 | Công cụ | Chờ Claude in Chrome kết nối rồi mới bắt đầu; dùng Chrome cho luồng giao diện, HTTP/SQL chỉ để đối chiếu dữ liệu |

## Checklist

### Chuẩn bị
- [x] Commit mốc trước khi rà (`cb73204`)
- [x] Sao lưu `petcare.db`
- [x] Chrome kết nối; server `uvicorn app.main:app --port 8000` với `AI_PROVIDER=gemini`
- [x] Ghi bảng quota trước lượt rà

### A. Luồng nghiệp vụ chuẩn (Chrome)
- [x] A1 Đăng nhập, đăng xuất 3 vai trò; menu và thẻ trang chủ theo vai trò
- [x] A2 Chủ nuôi + thú cưng: tạo (form thật + 20 ca biên), xem chi tiết
- [ ] A2b Tìm không dấu, xóa qua trang xác nhận — **chưa rà lượt này**
- [x] A3 Dịch vụ: quản lý thêm (13 ca biên giá/thời lượng); lễ tân và chăm sóc chỉ xem (403)
- [ ] A3b Ngưng bán/bán lại, tạo gói — **chưa rà lượt này** (chỉ thử id không tồn tại → H-03)
- [x] A4 Lịch hẹn: đặt, trùng lịch (giao nhau, liền kề), đổi, hủy có lý do, gợi ý khung trống, "Lịch của tôi"
- [x] A5 Hồ sơ chăm sóc: ghi, lịch chuyển "Hoàn thành", hồ sơ mới có trong bản tóm tắt AI
- [x] A6 Tiêm phòng: ghi mũi, danh sách đến hạn, quá hạn, gộp tên khác dấu
- [x] A7 Hóa đơn: lập, thu một phần, hủy, lập lại (thu nốt bị thay bằng ca đồng thời → H-01)
- [x] A8 Thống kê: chọn kỳ 19/09 → lộ số liệu sai do H-01, H-02
- [ ] A8b Kỳ mặc định, trạng thái rỗng — **chưa rà lượt này** (có test tích hợp)
- [x] A9 AI (Gemini thật): nhắc lịch hẹn (sửa, Chốt, Sao chép), nhắc tiêm, tóm tắt, hỏi đáp, trang quota, xoay model

### B. Phân quyền
- [x] Lễ tân, chăm sóc gõ thẳng URL và gửi form của vai trò trên (tài khoản, bảng giá, thống kê, quota, thu tiền, nhắc lịch)
- [x] Chưa đăng nhập mở mọi trang → về `/login`

### C. Dữ liệu biên và sai
- [x] Ô trống, chỉ khoảng trắng, chuỗi rất dài, ký tự đặc biệt, có dấu
- [x] Số âm/0/rất lớn (cân nặng, giá, tiền thu, thu quá số còn nợ)
- [x] Ngày sai định dạng, ngày quá khứ, ngày sinh tương lai, giờ ngoài giờ mở cửa (S6), ngày sai khi POST (S7)

### D. Bảo mật cơ bản
- [x] Chèn `<script>`/HTML vào ô nhập → hiện ra có bị thực thi không
- [x] `?tu=` trỏ ra ngoài; bấm đúp nút gửi; F5 sau POST; đăng xuất rồi Back

### E. Nhất quán số liệu
- [x] Tiền đã thu so với doanh thu thống kê → **lệch** (H-01)
- [ ] Hủy hóa đơn/hủy lịch phản ánh đúng trên thống kê — **chưa đối chiếu**
- [x] Hồ sơ vừa ghi → trang thú cưng → bản tóm tắt AI; mũi tiêm vừa ghi → danh sách đến hạn
- [x] `ai_logs` và `ai_quota` khớp số lần gọi thật; `prompt` không chứa dữ liệu liên hệ

### Kết thúc
- [x] Báo cáo + ảnh chụp lỗi, lỗi xếp theo mức nghiêm trọng
- [x] pytest toàn bộ (số thật), `codebase-map.md`, log phiên
- [x] Hỏi người dùng chọn lỗi cần sửa — người dùng chọn 3 lỗi cao; sửa theo [`2026-09-19-sua-loi-cao-ra-soat.md`](2026-09-19-sua-loi-cao-ra-soat.md)
