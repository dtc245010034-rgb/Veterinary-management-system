# Phien 2026-09-13-01

- **Ngay:** 2026-09-13
- **Muc tieu phien:** Đọc lại toàn bộ dự án và báo cáo hiện trạng; sau đó **dọn hết năm việc P6 để
  lại** trước khi mở P7.

## Quyet dinh

1. **Người dùng chốt ba hướng** (mình đề xuất, người dùng chọn đúng cả ba): bước xác nhận =
   **trang riêng** chứ không phải ô "tôi chắc chắn"; phạm vi = **ba thao tác làm mất dữ liệu**;
   thời gian test = **nới ngân sách** chứ không đổi fixture `db`. Chép vào
   [`../plans/2026-09-13-viec-ton-p6.md`](../plans/2026-09-13-viec-ton-p6.md).

2. **Roadmap ghi thừa hai thao tác — đọc code mới thấy.** "Khóa tài khoản" **hoàn tác được** (có nút
   Mở khóa ngay cạnh) và "Hủy lịch" **đã bắt gõ lý do** mới bấm được. Báo người dùng trước khi hỏi
   phạm vi, thay vì cứ thế làm đủ năm chỗ như tài liệu ghi. Đúng lớp lỗi của bài học 2 mục 9, lần
   này tài liệu sai chứ không phải code.

3. **GET render trang xác nhận, POST giữ nguyên chữ ký cũ.** Nhờ vậy **không phải sửa một test cũ
   nào** — mọi test integration đang POST thẳng vẫn chạy đúng luồng cũ. Nếu đổi POST thành hai bước
   thì hàng chục test phải sửa theo, và test sửa theo code là test hết tác dụng.

4. **Không tự tick ô smoke nào.** 5 ô mới của khối "Dọn việc tồn P6" để trống chờ người dùng bấm.

5. **Báo cáo đặt tên `2026-09-13-P6-viec-ton.md`, không phải `viec-ton-p6.md`.** Phép canh README
   neo dòng Trạng thái vào **mã phase trong tên báo cáo mới nhất**; tên không nêu phase làm nó đỏ.
   Phiên 11/09 đã gặp đúng chuyện này và né bằng cách không viết báo cáo — lần này giữ báo cáo và
   đổi tên, vì đợt dọn có đột biến và số đo cần chỗ ghi.

## Ket qua ra soat dau phien

| Kiểm | Kết quả |
|---|---|
| Test | **516 ca, 515 xanh, 1 đỏ** — đúng phép canh đếm log phiên, do hook vừa tạo file log mới. Không phải lỗi hệ thống |
| Phase | P0→P6 xong; đề bài mục 3.1 **8/8** chức năng; mục 3.2 (AI) **0/3** |
| Repo | Sạch, commit gần nhất `0c48955` |

## File da thay doi

**Mới:** `app/templates/xac_nhan.html`, `docs/plans/2026-09-13-viec-ton-p6.md`,
`docs/testing/reports/2026-09-13-P6-viec-ton.md`.

**Sửa:** `app/routers/invoices.py` + `app/routers/owners.py` (3 route GET xác nhận),
`app/templates/invoice_detail.html` + `app/templates/owner_detail.html` (nút → link GET),
`app/static/style.css` (`.the-xac-nhan`, `.hang-nut`, `a.nguy-hiem`), `app/config.py` + `.env.example`
(tên model Gemini), `tests/unit/test_architecture.py` (phép canh `codebase-map` so đuôi đường dẫn),
`tests/integration/test_invoices.py` + `tests/integration/test_owners.py` (6 test mới),
`docs/codebase-map.md`, `docs/roadmap.md`, `docs/testing/test-cases.md`,
`docs/testing/test-strategy.md`, `docs/testing/smoke-checklist.md`.

## Ket qua test

Đỏ trước, xanh sau — cả hai việc có code:

| Việc | Đỏ trước | Xanh sau |
|---|---|---|
| Phép canh `codebase-map` | Tạo tạm `app/routers/text.py`: phép canh **cũ xanh** (đó là bug), phép canh **mới đỏ** — và bắt thêm chỗ thật `app/auth.py` | 14 phép canh xanh |
| Trang xác nhận | 6 test mới đỏ: 405/404 vì chưa có route GET, HTML chưa có link | 6 xanh |

Đột biến, hoàn nguyên có so từng byte:

| # | Đột biến | Test đỏ |
|---|---|---|
| M1 | Bỏ dòng cảnh báo trong `xac_nhan.html` | 2 |
| M2 | Cho GET gọi luôn `xoa_thu_cung` | 1 |

**M2 dính đúng bẫy của bài học 3:** `owners.py` dùng CRLF, chuỗi tìm-thay viết kiểu LF không khớp,
script báo `chuoi cu xuat hien 0 lan` và dừng. Không có phép tự khẳng định đó thì kết quả đọc ra sẽ
là "không test nào đỏ" trong khi code còn nguyên vẹn.

```
522 passed
tests/unit          336 passed in 30.50s
tests/integration   185 passed in 64.57s   (máy đang bận, ba tầng chạy liên tiếp)
tests/e2e             1 passed in  9.45s
```

## Phat hien dang ghi

**`gemini-2.0-flash` đã bị Google tắt.** Tên đặt từ P0, nay nằm trong mục "Previous models —
Shut down" của [tài liệu Gemini API](https://ai.google.dev/gemini-api/docs/models). Đổi
`.env.example` và `config.py` sang `gemini-2.5-flash`. Đây đúng thứ việc 5 sinh ra để bắt — nếu để
tới lúc viết `gemini.py` thì nó hiện ra dưới dạng lỗi 404 giữa lúc đang debug prompt.

## Con do / phien sau lam gi

- ~~5 ô smoke khối "Dọn việc tồn P6" chờ người dùng bấm~~ → **tick đủ 5/5**, xem phần 2.
- **Không còn việc nào chờ người dùng quyết.** Năm việc P6 để lại đã ✅ hết trong `roadmap.md` mục P7.
- Phase kế tiếp: **P7 — tích hợp AI** (TC-082→100, bước 10 của TC-101, 20 ca guardrail G-01→G-20
  chạy tay với Gemini thật, bảng `ai_logs`). Việc đầu tiên: đọc [`../ai-safety.md`](../ai-safety.md)
  và mục P7 của [`../roadmap.md`](../roadmap.md), lập kế hoạch cho người dùng duyệt **trước khi viết
  code**. Cần **khóa API Gemini của người dùng**, và **gọi thử một lần** để xác nhận
  `gemini-2.5-flash` chạy được trước khi viết `app/ai/gemini.py`.
- P8 vẫn giữ một việc của P6: CSDL trống thì form đặt lịch không có câu hướng dẫn nào.

---

# Phan 2 — Smoke tick du va dong phien (13/09)

## Kết quả rà soát sau khi người dùng tick smoke

| Kiểm | Cách kiểm | Kết quả |
|---|---|---|
| Smoke | đếm ô trong repo | **117 tick / 21 trống** — 5 ô của đợt dọn đã tick đủ; cả 21 ô trống thuộc P7 (15) và P8 (6) |
| Test | `pytest` toàn bộ | **522 passed, 0 failed, 0 skipped** in 65,6s |
| Ma trận truy vết | script đối chiếu với code test | 80 ✅ · 1 🟡 · 20 ⬜ · 1 ➖ = 102. **133 trích dẫn file/hàm test đều tồn tại**; mọi TC ✅ đều được nhắc mã trong code |
| ERD ↔ model | phép canh `test_erd_khop_model_tung_cot` | Khớp, còn `ai_logs` của P7 (12/13 bảng) |
| Ba con số đếm file | đếm thật | plans **11**, sessions **10**, reports **18** — khớp bản đồ |
| Phép canh kiến trúc | đếm hàm test | **14**, khớp con số ghi trong bản đồ |
| README ↔ phase | phép canh tự động | Khớp |
| CLAUDE.md mục 9 | đếm | 4/5 dòng — khớp tiêu đề, **không thêm dòng nào phiên này** |

Không phát hiện chỗ nào tài liệu lệch code. Ba file đã cập nhật theo kết quả smoke: báo cáo đợt dọn
(mục 8 và 9), `roadmap.md` (dòng tổng kết), kế hoạch đợt dọn (DoD → ✅ ĐÓNG).

## Vì sao phiên này không thêm bài học vào CLAUDE.md mục 9

Hai chuyện đáng ghi đều **đã có dòng của nó**: bẫy CRLF khi chạy đột biến là bài học 3, và "tài liệu
mô tả thứ code không làm" (roadmap ghi thừa hai thao tác) là bài học 2. Trần cứng 5 dòng vẫn còn
chỗ, nhưng thêm dòng trùng ý chỉ làm danh sách dài ra mà không canh thêm được gì.

## Chốt tiến độ

| Hạng mục | Số liệu |
|---|---|
| Phase | P0 → **P6 xong**, cộng đợt dọn việc tồn 13/09. Còn **P7** (AI) và **P8** (nộp) |
| Test | **522 xanh** — unit 336, integration 185, e2e 1; không test nào bị skip trên máy này |
| Ma trận truy vết | 80 ✅ · 1 🟡 · 20 ⬜ · 1 ➖ / 102. 🟡 là TC-101 (e2e thiếu bước 10 của P7); 20 ⬜ là P7 (19 ca AI) + P8 (TC-102) |
| Smoke | 117 tick / 21 trống (P7: 15, P8: 6) |
| ERD | 12/13 bảng đã dựng; còn `ai_logs` của P7 |
| Chức năng đề bài mục 3.1 | **8/8 xong.** Mục 3.2 (3 tính năng AI): **0/3** — toàn bộ là P7 |
| Việc chờ người dùng quyết | **Không còn** |

## Phien sau bat dau tu dau

1. Đọc [`../codebase-map.md`](../codebase-map.md) — dòng đầu có trạng thái, khối **"Làm tiếp"** chỉ
   thẳng sang P7.
2. Đọc **log này, phần 2** — đủ để nắm tình hình; phần 1 nếu cần chi tiết đợt dọn việc tồn.
3. Đọc mục **P7** trong [`../roadmap.md`](../roadmap.md) — năm việc P6 để lại nay ✅ hết, nhưng việc
   số 5 còn một nửa: **phải gọi thử Gemini bằng khóa thật** trước khi viết `app/ai/gemini.py`.
4. Kế hoạch gần nhất đã đóng: [`../plans/2026-09-13-viec-ton-p6.md`](../plans/2026-09-13-viec-ton-p6.md)
   (✅ ĐÓNG). Không còn kế hoạch nào dang dở.
5. **Việc kế tiếp: lập kế hoạch P7 cho người dùng duyệt** — đọc [`../ai-safety.md`](../ai-safety.md)
   (system prompt, `DISCLAIMER`, 20 ca G-01→G-20 viết sẵn từ P0) trước khi thiết kế.

**Bẫy đã biết, đừng mất thời gian chẩn đoán lại:**

- Chạy `pytest` ngay đầu phiên mà phép canh **đếm log phiên** đỏ → do hook `SessionStart` vừa tạo
  file log mới. Cập nhật con số trong `codebase-map.md` khi đóng phiên, **không phải lỗi code**.
- Báo cáo mới trong `testing/reports/` **phải có mã phase trong tên file** (`YYYY-MM-DD-Pn-*.md`),
  nếu không phép canh README đỏ.
- `app/routers/owners.py` dùng **CRLF**: script tìm-thay viết kiểu LF sẽ không khớp, và kết quả đọc
  ra là "không test nào đỏ" trong khi code còn nguyên.
- Tên model Gemini: `gemini-2.0-flash` **đã chết**, dùng `gemini-2.5-flash`.
