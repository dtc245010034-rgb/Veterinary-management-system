# Phien 2026-09-06-01

- **Ngay:** 2026-09-06
- **Muc tieu phien:** P5 chặng 1 — hóa đơn và thanh toán (US-19, US-20; TC-065 → TC-073),
  theo kế hoạch đã duyệt [`../plans/2026-09-06-p5-hoa-don-va-thanh-toan.md`](../plans/2026-09-06-p5-hoa-don-va-thanh-toan.md).

## Quyet dinh

1. **Sửa TC-066 trước khi viết dòng code nào.** Câu cũ "tổng bằng tổng các dòng" nghe như kiểm phép
   cộng nhiều số hạng, trong khi ở P5 hóa đơn luôn chỉ có một dòng. Viết lại thành thứ kiểm được
   thật: `total_amount` bằng `qty × unit_price` của dòng dịch vụ, và không đọc lại `services.price`.
   Kèm một ghi chú phạm vi công khai dưới US-19 thay vì lặng lẽ để tài liệu lệch code.

2. **Giữ ràng buộc UNIQUE `appointment_id`, chấp nhận hệ quả.** Test viết ban đầu mong "hủy hóa đơn
   nhầm rồi lập lại được"; điều đó mâu thuẫn với UNIQUE đã chốt trong kế hoạch. Giữ UNIQUE vì đó là
   thứ duy nhất chặn được hai request cùng lúc, và viết lại test để kiểm hệ thống **lộ lỗi tử tế**:
   ra `LoiNghiepVu` nêu mã hóa đơn cũ, không phải màn hình đen.

3. **Phép canh kiến trúc chỉ nhận ba chuỗi tiền, không nhận `cancelled`.** Kế hoạch viết bốn, nhưng
   `cancelled` cũng là trạng thái lịch hẹn ở bốn file khác. Canh cả bốn thì phải mở bốn ngoại lệ, và
   một phép canh đầy ngoại lệ thì lần sau không ai tin.

4. **Việc LẬP hóa đơn nằm ở router lịch hẹn, không ở router hóa đơn.** Nó xuất phát từ một dòng trên
   lưới lịch; khi bị từ chối, người dùng phải quay về đúng lưới ngày họ đang xem kèm thông báo.

5. **Dữ liệu mẫu đi qua `billing.py` chứ không dựng model tay** — trạng thái hóa đơn chỉ được quyết
   ở một chỗ, seed không phải ngoại lệ. Phép canh mới ở mục 3 cũng buộc phải như vậy.

## File da thay doi

**Mới:** `app/models/invoice.py`, `app/models/payment.py`, `app/services/billing.py`,
`app/routers/invoices.py`, `app/templates/invoices.html`, `app/templates/invoice_detail.html`,
`tests/unit/test_billing_service.py`, `tests/integration/test_invoices.py`,
`docs/testing/reports/2026-09-06-P5-chang1.md`.

**Sửa:** `app/models/__init__.py`, `app/main.py`, `app/routers/appointments.py` (nút lập hóa đơn),
`app/templates.py` (filter `tien` dùng chung), `app/templates/base.html` (link menu),
`app/templates/appointments.html`, `app/templates/services.html` (macro `tien` → filter),
`app/static/style.css` (3 nhãn trạng thái + `tfoot`), `app/seed.py`,
`tests/unit/test_architecture.py` (phép canh thứ 9), `docs/codebase-map.md`,
`docs/testing/test-cases.md`, `docs/testing/smoke-checklist.md`, `docs/user-stories.md`,
`docs/plans/2026-09-06-p5-hoa-don-va-thanh-toan.md`.

## Ket qua test

```
454 passed in 60.45s

tests/unit          308 passed in 22.94s
tests/integration   145 passed in 34.64s
tests/e2e             1 passed in  6.08s
```

**13 đột biến, cả 13 đều bị bắt** — 7 ở tầng nghiệp vụ, 6 ở tầng HTTP. Chi tiết trong
[`../testing/reports/2026-09-06-P5-chang1.md`](../testing/reports/2026-09-06-P5-chang1.md).

Tầng unit vượt ngưỡng xem lại 20s. Đã kiểm chứng bằng worktree ở commit `9876fcc`: **cùng bộ test
cũ, cùng máy, hôm nay chạy 21,54s trong khi sáng nay chạy 13,29s**. Phần vượt là của máy, không phải
của test mới. Đo lại ở P6 trên máy rảnh trước khi cắt gì.

## Con do / phien sau lam gi

- **P5 chặng 2:** US-21 trong `scheduling.huy_lich` (TC-074, TC-075), nối bước 7→9 của TC-101, báo
  cáo P5 đầy đủ. Bước 7 của kế hoạch trở đi.
- **15 ô smoke chặng 1 đang trống** trong [`../testing/smoke-checklist.md`](../testing/smoke-checklist.md),
  chờ người dùng bấm trên trình duyệt thật. CSDL đã seed sẵn đúng trạng thái cần: một hóa đơn đã thu
  đủ, một thu một phần, một buổi đã xong chưa lập hóa đơn.
- **10 ô smoke của hai đợt sửa lỗi ngày 06/09** vẫn trống từ phiên trước.
- **Lỗi cũ chưa sửa, không thuộc P5:** link **Thống kê** trên thanh điều hướng của `manager` trỏ tới
  `/stats` chưa tồn tại → 404. Có từ P1, để P6 đóng lại cùng lúc với việc dựng trang.

---

# Phần 2 — Rà luồng P5 chặng 1 bằng trình duyệt (07/09)

Người dùng yêu cầu rà luồng bằng Claude in Chrome trước khi họ smoke test.

## Tám lỗi tìm được, tất cả đã sửa

Chi tiết: [`../testing/reports/2026-09-07-ra-luong-P5-chang1.md`](../testing/reports/2026-09-07-ra-luong-P5-chang1.md).

| # | Lỗi | Sửa ở |
|---|---|---|
| 1 | **Không đổi được giá dịch vụ bằng chuột** — route `/sua` có từ P2b nhưng không form nào trỏ tới | `templates/services.html` |
| 2 | Thông báo lỗi lộ chữ "P5" cho người dùng | `services/billing.py` + phép canh mới |
| 3 | Hóa đơn đã hủy vẫn hiện "Còn nợ 150.000đ" | `models/invoice.py` — sửa ở property, không ở template |
| 4 | Hóa đơn đã hủy là ngõ cụt câm | `templates/appointments.html`, `invoice_detail.html` |
| 5 | Trang chủ thiếu thẻ "Hóa đơn" — lần thứ ba của cùng lớp lỗi | `templates/home.html` + phép canh mới |
| 6 | Buổi chưa lập hóa đơn nằm cách đây một tháng, không ai tìm ra nút | `app/seed.py` |
| 7 | "Xem hồ sơ" và "Xem hóa đơn" dính nhau | `static/style.css` |
| 8 | Thanh điều hướng quản lý vỡ từ ~1040px, trước P5 là ~986px | `static/style.css` |

Lỗi 1 quan trọng nhất: nó chặn đứng ô smoke chứng minh quyết định giá trị nhất của P5. Sau khi sửa,
đã kiểm bằng chuột: đổi giá 150.000 → 200.000đ, hóa đơn cũ **vẫn 150.000đ**.

## Hai bài học thành máy canh, không thành dòng ghi nhớ

Danh sách CLAUDE.md mục 9 vẫn **4/5 dòng** — cả hai lỗi lặp lần này đều tự động hóa được:

- `test_thong_bao_loi_nghiep_vu_khong_lot_ma_phase_ra_ngoai` — thông báo cho người dùng không được
  chứa mã phase.
- `test_moi_link_tren_thanh_dieu_huong_deu_co_the_tren_trang_chu` — thêm link menu mà quên trang chủ
  thì đỏ ngay.

Phép canh mã phase viết lần đầu **xanh trên chính ca đã sinh ra nó** (regex dừng ở dấu ngoặc đầu tiên
của f-string). Lần thứ hai một phép canh mới bỏ sót ca gốc của mình — phải luôn thử làm hỏng lại ca
gốc sau khi viết phép canh.

## Kết quả test

```
464 passed in 58.16s
```

13 đột biến ở đợt trước + 8 đột biến đợt này, tất cả đều bị bắt đúng test.

## Việc dọn thêm

Script python ghi đè file đã đổi ký tự xuống dòng của 70 file (repo trộn LF và CRLF).
Đã trả từng file về đúng kiểu của bản trong git; diff cuối cùng còn đúng số file thật sự sửa.

## Còn dở

- ~~24 ô smoke đang trống~~ — người dùng đã tick đủ, xem Phần 3. Gồm 15 ô chặng 1, 9 ô đợt sửa lỗi 07/09, và 10 ô của hai đợt sửa lỗi
  ngày 06/09 từ phiên trước.
- **P5 chặng 2** chưa bắt đầu: US-21 (TC-074, TC-075) và bước 7→9 của e2e.
- Link "Thống kê" vẫn trỏ tới `/stats` chưa tồn tại — thuộc P6.

---

# Phần 3 — Rà soát toàn hệ thống sau khi người dùng tick smoke (07/09)

## Người dùng đã tick

**90 ô đã tick / 31 ô còn trống.** Đã đối chiếu: 31 ô còn lại đều thuộc phần **chưa làm** —
P5 chặng 2 (2 ô), P6 (8), P7 (15), P8 (6). Không còn ô nào của phần đã làm bị bỏ trống, gồm cả
10 ô tồn đọng từ hai đợt sửa lỗi 06/09 và 9 ô của đợt rà 07/09.

## Kết quả rà soát

| Kiểm | Kết quả |
|---|---|
| Test | **465 passed** — unit 311 (21,74s), integration 153 (30,91s), e2e 1 (4,88s) |
| Ma trận truy vết | 69 ✅ · 4 🟡 · 29 ⬜ = 102; mọi file test được trích dẫn đều tồn tại |
| ERD vs code | 12/13 bảng đã dựng, không bảng nào lệch; còn `ai_logs` (P7) |
| Kế hoạch | 8/8 kế hoạch cũ đóng hết; P5 còn 11 ô (4 bước chặng 2 + 7 ô DoD) |
| CLAUDE.md mục 9 | tiêu đề nói 4/5, đếm được 4 — khớp |
| Phép canh kiến trúc | 11 phép canh, xanh |

## Hai chỗ tài liệu lệch code, đã sửa

`docs/architecture.md` viết từ KT1 với **tên hàm dự kiến bằng tiếng Anh**, không ai soát lại sau khi
code xong:

- Luồng 1 (đặt lịch, làm ở P3): `create_appointment` → thật là `dat_lich`; `ConflictError` → thật là
  `TrungLich`; "201 + chi tiết lịch" → thật là 303 về lưới lịch.
- Luồng 2 (hóa đơn, làm hôm qua): `create_invoice`/`record_payment` → thật là `lap_hoa_don`/
  `ghi_nhan_thanh_toan`; `POST /invoices` → thật là `POST /appointments/{id}/hoa-don`;
  `POST /invoices/{id}/payments` → thật là `POST /invoices/{id}/thanh-toan`.

Đây đúng loại lệch mà cả dự án lấy làm luận điểm chính để chống: báo cáo cuối kỳ dựng từ tài liệu,
tài liệu mô tả hàm không tồn tại thì báo cáo sai. Luồng 3 (AI) giữ nguyên vì P7 chưa làm — đó là mô
tả dự kiến, không phải mô tả sai.

Luồng 2 còn được bổ sung hai câu nói rõ vì sao việc **lập** hóa đơn nằm ở `routers/appointments.py`.

## Còn dở

- **P5 chặng 2**: US-21 trong `scheduling.huy_lich` (TC-074, TC-075), nối bước 7→9 của TC-101 lên
  9/11, báo cáo P5 đầy đủ. Bước 7 của kế hoạch trở đi.
- Link **Thống kê** vẫn trỏ tới `/stats` chưa tồn tại → 404. Thuộc P6.
