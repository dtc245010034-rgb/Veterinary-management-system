# Phiên 2026-09-05-02

- **Ngày:** 2026-09-05
- **Mục tiêu phiên:** P3 chặng 2 — đổi lịch, hủy lịch, xem lịch theo vai trò (TC-044→052, TC-009)

## Quyết định

**Kiểm xong hết mới ghi.** Đây là quyết định quan trọng nhất của chặng. TC-045 yêu cầu khi đổi lịch
thất bại thì lịch giữ nguyên giờ cũ, nên `doi_lich()` chạy đủ bốn phép kiểm — trạng thái, quá khứ,
nhân viên mới, trùng lịch — rồi mới gán `start_at`. Viết ngược lại sẽ để lịch ở trạng thái nửa vời:
giờ đã đổi nhưng thao tác báo lỗi. Test đọc lại từ CSDL sau khi bắt exception chứ không tin vào đối
tượng trong bộ nhớ.

**`bo_qua_id` từ chặng 1 dùng nguyên vẹn, không phải sửa logic chống trùng.** Đúng như đã dự trù.
Thử nghiệm đột biến cho thấy bỏ nó đi thì không lịch nào đổi được giờ **và cũng không đổi được nhân
viên** — nó tự báo trùng với chính mình.

**Lý do hủy là bắt buộc.** Khung giờ bị giải phóng mà không ai biết vì sao thì sau này không truy
nguyên được, và thống kê ở P6 không phân biệt được khách hủy với lỗi vận hành.

**Chặn `caretaker` bằng chuyển hướng, không bằng ẩn menu.** `GET /appointments` đưa `caretaker` sang
`/appointments/cua-toi`, và trang đó lọc theo `user.id` chứ không nhận `nhan_vien_id` từ URL. Ẩn link
trong menu là chưa đủ — gõ thẳng URL vẫn ra lịch của tất cả mọi người. Đây là chỗ TC-009 hoãn từ P1
được trả nợ.

**Đổi lịch cho phép đổi cả nhân viên, không chỉ giờ** — US-12 nói vậy. Nhân viên mới đi qua đúng
phép kiểm như lúc đặt lịch, nên tách `_kiem_nhan_vien()` dùng chung với `dat_lich()`.

**Hai route dùng chung một template.** Cờ `chi_cua_toi` ẩn bộ lọc nhân viên và đổi tiêu đề; cột
"Thao tác" chỉ hiện với `manager` và `receptionist`. Không dựng template thứ hai cho một khác biệt
nhỏ như vậy.

## File đã thay đổi

**Ứng dụng**
- `app/services/scheduling.py` — thêm `doi_lich()`, `huy_lich()`, `_kiem_nhan_vien()`,
  `_chan_neu_khong_sua_duoc()`, hằng `TRANG_THAI_SUA_DUOC`
- `app/routers/appointments.py` — `GET /appointments/cua-toi`, `POST /{id}/doi`, `POST /{id}/huy`,
  chuyển hướng `caretaker`, gộp xử lý lỗi thành `_bao_loi()` và `_ve_lich()`
- `app/templates/appointments.html` — cột "Thao tác", tiêu đề và bộ lọc theo cờ `chi_cua_toi`
- `app/static/style.css` — kiểu cho cột thao tác và nút hủy

**Kiểm thử**
- `tests/unit/test_scheduling.py` — thêm 13 test (đổi lịch 8, hủy lịch 5)
- `tests/integration/test_appointments.py` — thêm 10 test (đổi/hủy qua HTTP, lịch theo vai trò)

**Tài liệu**
- `docs/testing/reports/2026-09-05-P3-chang2.md` (mới)
- `docs/testing/test-cases.md` — 10 dòng chuyển ✅
- `docs/testing/smoke-checklist.md` — khối P3 chặng 2 đã cài đặt xong, sẵn sàng tick
- `docs/plans/2026-09-05-p3-lich-hen.md` — tick bước 8→11 và 6/7 mục DoD
- `docs/codebase-map.md` — 5 dòng cập nhật vai trò file

## Kết quả test

```
$ pytest
233 passed in 22.14s

unit         153 passed in  9.09s   (ngân sách < 15s — đạt)
integration   80 passed in 15.52s   (ngân sách < 30s — đạt)
```

**Hai vòng đỏ–xanh, mỗi vòng xác nhận đỏ trước:**

| Vòng | Đỏ vì | Xanh sau khi |
|---|---|---|
| 1 | 13 test đỏ: `module 'app.services.scheduling' has no attribute 'huy_lich'` | viết `doi_lich()` + `huy_lich()` |
| 2 | 8 test đỏ: `assert 404 == 200` cho `/appointments/cua-toi` và các route đổi/hủy | viết 3 route + cột thao tác |

**Ba thử nghiệm đột biến** chứng minh test mới bắt được lỗi:

| Đột biến | Test đỏ |
|---|---|
| Gán `start_at` **trước** khi kiểm trùng | `test_doi_sang_khung_ban_bi_tu_choi_va_giu_nguyen_gio_cu` + 1 |
| Bỏ `bo_qua_id=lich.id` | `test_doi_lich_khong_tu_so_sanh_voi_chinh_no`, `test_doi_sang_nhan_vien_khac` |
| Bỏ chuyển hướng `caretaker` | `test_caretaker_go_thang_trang_lich_chung_van_chi_thay_lich_minh` |

**Kiểm chứng trên server thật:** đổi sang khung trống → 303, trạng thái "Đã đổi lịch"; đổi sang khung
bận của chính nhân viên đó → 400 kèm lý do và gợi ý `08:00 08:30 09:00 09:30 10:00`, **lịch giữ
nguyên 10:00–10:45**; hủy không lý do → 400; hủy kèm lý do → 303 và đặt lại được đúng khung vừa hủy;
`chamsoc1` gõ thẳng `/appointments` → chuyển sang `/appointments/cua-toi`, chỉ thấy lịch của mình,
không có nút Hủy, và `POST .../huy` trả 403.

**Một sai sót, lại ở phía kiểm thử chứ không ở code.** Script kiểm chứng lần đầu chọn "khung đã bận"
là lịch của **nhân viên khác** rồi kỳ vọng bị từ chối; hệ thống trả 303 — và 303 mới là đúng. Sửa
script chọn hai lịch cùng một nhân viên thì từ chối đúng như mong đợi. Lần thứ ba trong dự án một
"lỗi" hóa ra nằm ở test/script (trước đó: helper lấy id theo chỉ số ở P2b, tên có dấu viết cứng ở
chặng 1). Cùng một gốc: giả định về dữ liệu thay vì đọc từ nguồn.

## Còn dở / phiên sau làm gì

**Khối smoke "P3 chặng 2" (6 ô) chưa ai tick.** Chức năng đã có đủ để kiểm: cột "Thao tác" trên lưới
lịch có ô giờ + ô chọn nhân viên kèm nút Đổi, và ô lý do kèm nút Hủy. Đăng nhập `chamsoc1` /
`matkhau123` để kiểm hai ô cuối. Đây là mục DoD duy nhất của P3 còn để trống.

**Phiên sau: P4 — hồ sơ chăm sóc và lịch tiêm nhắc lại.** TC-053 → TC-064 (12 ca), và TC-020 hoãn từ
P2a (trang chi tiết thú cưng hiện lịch sử chăm sóc và lịch tiêm) sẽ tick được ở đó.

Lưu ý cho P4:
- P4 là nơi lịch chuyển sang trạng thái `done`. Chuyển xong thì lịch **không đổi, không hủy được
  nữa** — `TRANG_THAI_SUA_DUOC` trong `scheduling.py` đã chặn sẵn, P4 chỉ cần gọi đúng.
- `vaccinations.next_due_at` là nguồn của danh sách nhắc tiêm. Dùng `clock.now()` để test cố định
  được mốc "đến hạn trong 30 ngày", đừng dùng `datetime.now()`.
- Tầng unit đang ở 9,09s trên ngân sách 15s, và P4 thêm 2 bảng nữa vào fixture `db`. Ngưỡng xem lại
  là 20s — nếu chạm thì chuyển fixture sang tạo schema một lần cho cả phiên rồi rollback từng test.

---

# Phần bổ sung — trả nợ kiến trúc và bao phủ (cùng phiên)

## Quyết định

**Rà soát toàn dự án sau P3 trước khi vào P4.** 15 phép kiểm, 11 khớp, 4 lệch. Hai điểm tài liệu sửa
ngay (commit `cdeafe3`): `codebase-map.md` liệt kê từng file P0 rồi dừng nên thiếu 13 file sinh sau —
chuyển sang liệt kê theo thư mục để không trôi tiếp; `architecture.md` mô tả `app/schemas/` (Pydantic,
lỗi 422) **chưa bao giờ tồn tại** — thực tế dùng `Form()` và trả 400, đã đánh dấu `app/schemas/` là
bỏ hẳn thay vì "chờ P2+".

**Trả nợ trước khi vào P4, theo quyết định của người dùng.** Lý do: P4 thêm hai bảng và hai router
nữa, nợ kiến trúc để càng lâu càng đắt.

**Tách `app/services/users.py`, chuyển nguyên vẹn bốn thao tác, không thêm quy tắc mới.** Router chỉ
còn HTTP như ba router viết sau. 14 integration test của P1 không sửa dòng nào và vẫn xanh — đó là
điều kiện để gọi đây là refactor chứ không phải viết lại.

**Test viết sau code thì phải chứng minh bằng đột biến.** 10 hàm được bù test đã tồn tại sẵn nên test
xanh ngay lần đầu, không chứng minh gì theo luật 2 chống test giả. Sáu đột biến, mỗi cái làm đỏ đúng
một test và đúng cái dự kiến.

**Ranh giới kiến trúc phải kiểm được bằng lệnh, không bằng trí nhớ.** Nợ này phát sinh ở P1 khi
`app/services/` mới chỉ có `clock.py` và `text.py`. Phép kiểm rất rẻ:
`grep "db.add\|db.commit" app/routers/*.py` phải rỗng — nên chạy ở mỗi đợt rà soát cuối phase.

## File đã thay đổi

**Ứng dụng**
- `app/services/users.py` (mới) — 4 thao tác tài khoản
- `app/routers/users.py` — chỉ còn HTTP, bỏ `db.add`/`db.commit`/`select`/`hash_password`

**Kiểm thử**
- `tests/unit/test_users_service.py` (mới) — 11 test
- `tests/unit/test_owners_service.py` — thêm 14 test cho 6 hàm còn thiếu
- `tests/unit/test_catalog_service.py` — thêm 4 test cho `lay_goi`, `ban_lai_goi`
- `tests/unit/test_scheduling.py` — thêm 5 test gọi thẳng `tim_lich_trung`, `khung_gio_trong`

**Tài liệu**
- `docs/plans/2026-09-05-tra-no-kien-truc-va-bao-phu.md` (mới)
- `docs/testing/reports/2026-09-05-tra-no.md` (mới)
- `docs/codebase-map.md`, `docs/architecture.md`, `docs/testing/test-cases.md`

## Kết quả test

```
$ pytest
267 passed in 22.68s

unit         187 passed in 10.16s   (ngân sách < 15s — đạt, trước là 153/9,09s)
integration   80 passed in 15.03s   (ngân sách < 30s — đạt, giữ nguyên số test)
```

Integration giữ nguyên 80 test trong khi unit tăng 34 — đúng mục đích của việc tách tầng.

**Một vòng đỏ–xanh thật:** 11 test của `services/users.py` đỏ với
`ImportError: cannot import name 'users' from 'app.services'`, xanh sau khi viết service.

**Sáu thử nghiệm đột biến**, mỗi cái làm đỏ đúng một test dự kiến: bỏ chặn tự khóa mình · bỏ kiểm
trùng username · `sua_chu_nuoi` bỏ `_bat_buoc` · `sua_thu_cung` bỏ kiểm cân nặng · `ban_lai_goi` đặt
nhầm cờ · `khung_gio_trong` bỏ chặn hết giờ làm việc.

## Còn dở / phiên sau làm gì

**Khối smoke "P3 chặng 2" (6 ô) vẫn chưa ai tick.** Không liên quan tới phần trả nợ này — chức năng
đã sẵn sàng từ trước đó.

**Phiên sau: P4 — hồ sơ chăm sóc và tiêm phòng.** TC-053 → TC-064 (12 ca), cộng TC-020 hoãn từ P2a.

Lưu ý cho P4 (giữ nguyên từ phần trên, thêm một mục):
- P4 là nơi lịch chuyển `done`; `TRANG_THAI_SUA_DUOC` đã chặn sẵn việc đổi/hủy sau đó.
- `vaccinations.next_due_at` là nguồn nhắc tiêm; dùng `clock.now()`, không dùng `datetime.now()`.
- Tầng unit đang 10,16s trên ngân sách 15s. P4 thêm 2 bảng vào fixture `db`. Ngưỡng xem lại 20s.
- **Viết `app/services/care_records.py` và `vaccinations.py` ngay từ đầu**, đừng để logic lọt vào
  router như P1 đã làm. Kiểm bằng `grep "db.add\|db.commit" app/routers/*.py` trước khi đóng phiên.

---

# Phần bổ sung 2 — cơ chế học hỏi giữa các phiên (cùng phiên)

## Vấn đề

Người dùng hỏi: bài học rút ra được lưu ở đâu để phiên sau không bỏ qua như chưa từng biết.

Câu trả lời thật lúc đó: **không ở đâu bền cả.** Bài học nằm rải rác trong 7 file — 2 log phiên, 4
báo cáo, 1 kế hoạch — không file nào được nạp tự động. Và log phiên là biên niên: luật mục 6 chỉ bắt
đọc log *gần nhất*, nên bài học viết ở log 02 khuất tầm nhìn ngay khi có log 04. Tuổi thọ thực tế
một phiên.

Bằng chứng mạnh nhất cho thấy "viết ra" là không đủ: luật *"chỉ tick ô kiểm được thật"* viết từ P1
sau vụ `/stats`, **vẫn bị phạm lại ở P2 rồi P3**.

## Quyết định

**Không gộp cơ chế chặn với cơ chế nhắc.** Người dùng đề xuất gộp test và danh sách bài học thành một
cơ chế duy nhất. Phản biện đã trình bày: test dùng để **chặn** (tác động sau khi phạm lỗi), danh sách
bài học dùng để **quyết định** (tác động trước khi phạm lỗi). Gộp lại thì làm kém cả hai.

**Không viết vào CLAUDE.md dòng "nhớ chạy kiểm tra ranh giới".** `pytest` đã là luật đóng phiên ở mục
6, và mục 7 đã cấm xóa/skip test để lấy màn hình xanh. Thêm dòng nhắc sẽ biến một cơ chế **không phụ
thuộc trí nhớ** thành cơ chế **có vẻ phụ thuộc trí nhớ**.

**Thứ thật sự thiếu là luật phân loại, không phải luật chạy.** Không có gì bắt agent *thêm* bài học
mới. Đó mới là phần đưa vào CLAUDE.md.

**Trần cứng 5 dòng cho danh sách bài học.** Không trần thì sau vài phase nó thành nghĩa địa và không
ai đọc. Hiện dùng 3/5, để trống 2 — chính chỗ trống chứng minh cái trần có hiệu lực.

**Bỏ đề xuất hook chạy script rà soát.** Cảnh báo lặp mỗi phiên mà không cản đường sẽ bị ngó lơ sau
vài phiên, lúc đó tệ hơn không có. Thêm nữa, đợt rà soát chạy tay có hai kết quả sai (`.gitignore`
báo thiếu dù có; 15 tên test báo không tồn tại do regex cắt nhầm `.py`) — tôi ngồi đọc và loại được,
script tự chạy thì không. Mọi phép kiểm đáng giá trong đó đều thành test được.

## File đã thay đổi

- `tests/unit/test_architecture.py` (mới) — 6 phép kiểm, 22 test sau khi parametrize
- `CLAUDE.md` — thêm mục 9 "Bài học — luật phân loại" (173 → 203 dòng)
- `docs/testing/test-strategy.md` — thêm mục 3b giải thích nhóm test canh ranh giới
- `docs/codebase-map.md` — thêm `unit/test_architecture.py`

## Kết quả test

```
$ pytest
289 passed in 25.23s     (trước: 267)

tests/unit/test_architecture.py   22 passed in 0.30s
```

**Đỏ trước, thật sự:** lần chạy đầu tiên `test_moi_file_app_va_tests_deu_co_trong_codebase_map` đỏ
ngay — vì chính `test_architecture.py` chưa có trong `codebase-map.md`. Phép kiểm tự chứng minh nó
hoạt động ngay ở ca đầu tiên nó gặp.

**Sáu thử nghiệm đột biến**, mỗi cái làm đỏ đúng phép kiểm dự kiến:

| Đột biến | Test đỏ |
|---|---|
| Router gọi `db.commit()` | `test_router_khong_ghi_thang_xuong_csdl[owners.py]` |
| `services/clock.py` import fastapi | `test_services_khong_phu_thuoc_fastapi[clock.py]` |
| Router import thẳng `app.ai.gemini` | `test_router_khong_import_thang_vao_app_ai[owners.py]` |
| Thêm link trỏ tới file không có | `test_moi_link_tuong_doi_trong_tai_lieu_deu_ton_tai` |
| Thêm file `app/services/` mà quên ghi map | `test_moi_file_..._codebase_map` **và** `test_moi_ham_public_...` |
| Thêm hàm public không có test | `test_moi_ham_public_trong_services_deu_duoc_goi_thang_trong_test` |

Ca thứ năm làm đỏ hai test cùng lúc — đúng, vì một file mới chứa hàm public không có test vi phạm cả
hai ranh giới.

## Còn dở / phiên sau làm gì

**Khối smoke "P3 chặng 2" (6 ô)** vẫn chưa tick — không liên quan tới phần này.

**Rủi ro cần theo dõi:** phiên nào đó làm `test_architecture.py` đỏ vì lý do chính đáng rồi gỡ test
cho xanh. CLAUDE.md mục 7 đã cấm, và mỗi test có dòng "NẾU TEST NÀY ĐỎ: … đừng xóa test" chỉ đúng
cách sửa hợp lệ. Nếu P7 có nhu cầu thật khiến một phép kiểm thành sai, phải **sửa phép kiểm cho hẹp
lại và ghi lý do**, không xóa.

---

# Phần bổ sung 3 — smoke P3 và ô thứ tư bị tick nhầm

Người dùng tick đủ 6/6 khối "P3 chặng 2". Kiểm lại: **5 hợp lệ, 1 không kiểm được.**

Ô `Hủy lịch đã hoàn thành → bị chặn` đòi một lịch ở trạng thái `done`, mà không có đường nào dựng
được trạng thái đó trên giao diện: `'done'` chỉ xuất hiện trong `models/appointment.py` (hằng số,
nhãn, CHECK); `seed.py` không tạo lịch nào khác `booked`; chỉ có hai phép gán `status` trong
`scheduling.py` là `rescheduled` và `cancelled`. `done` do P4 đặt.

**Lần thứ tư, và là dạng khó thấy nhất.** Ba lần trước ô mô tả chức năng chưa tồn tại — đối chiếu
code là thấy. Lần này ô mô tả đúng chức năng đã có và chạy đúng; thứ chưa tồn tại là **trạng thái
tiền đề**. Lỗi thuộc về agent: tôi viết ở chính mục 8 báo cáo chặng 2 rằng "`done` do P4 đặt, P3 chỉ
đọc", rồi vẫn để ô đó trong khối giao cho người dùng.

Đáng ghi hơn cả: luật này vừa được đưa vào CLAUDE.md mục 9 trong **chính phiên này**, và vẫn bị phạm.
Đó là bằng chứng cho lập luận đã dùng để chọn cơ chế: luật chỉ mạnh khi có gì đó bắt buộc dừng lại
mà đọc. Dạng lỗi này chưa tự động hóa được, nên chỉ còn cách bổ sung câu hỏi kiểm vào luật —
*dựng được trạng thái ban đầu của ô này bằng giao diện hiện tại không?* — và chấp nhận rằng nó vẫn
phụ thuộc kỷ luật.

**Hành vi thì đã chứng minh:** TC-049 xanh và thử nghiệm đột biến cho thấy test đó bắt được lỗi. Chỉ
phép kiểm bằng mắt là chưa làm được.

Ô đã chuyển sang khối P4, diễn đạt chặt hơn: nút Đổi/Hủy phải biến mất **và** gọi thẳng
`POST /appointments/{id}/huy` phải bị chặn.

## Trạng thái P3

- Smoke: chặng 1 **8/8**, chặng 2 **5/5**
- DoD 7/7 — **P3 đóng**
- Ma trận: 47 ✅ · 4 🟡 · 51 ⬜

---

# Phần bổ sung 4 — P4 chặng 1: hồ sơ chăm sóc

## Quyết định

**Sửa spec trước khi viết code.** Ba sửa đổi tài liệu chạy trước dòng code đầu tiên: US-18 (khoảng
lấy danh sách đến hạn), TC-062 (theo US-18), ERD `vaccinations.given_at` (kiểm ở service, không
CHECK). US-18 bản gốc **tự mâu thuẫn** — "chỉ thấy hạn trong 30 ngày tới" và "quá hạn phải được đánh
dấu rõ" không thể cùng đúng. Sửa công khai thay vì lặng lẽ code khác spec.

**Ba trường suy ra từ lịch hẹn, không nhận từ form:** `pet_id`, `staff_id`, `performed_at`. Trong đó
`staff_id` là bẫy dễ mắc nhất — lấy `current_user.id` thì quản lý ghi hộ sẽ làm lịch sử ghi quản lý
là người tắm chó.

**`performed_at` dựa vào một ràng buộc của P3.** Nó là bản sao của `appointment.start_at`, không lệch
được vì lịch `done` không đổi giờ được nữa. Cảnh báo đã ghi trong docstring: nới
`TRANG_THAI_SUA_DUOC` thì chỗ này âm thầm sai.

**Chặn ghi hồ sơ cho lịch chưa diễn ra.** Kế hoạch đầu chỉ chặn lịch đã hủy — ca gần như không tới
được qua giao diện. Lỗ thật là lịch tuần sau: nút hiện, bấm vào là có hồ sơ cho buổi chưa xảy ra và
khóa luôn khả năng đổi/hủy.

**Dữ liệu mẫu phải dựng sẵn trạng thái, không chỉ dựng sẵn bản ghi.** Lần kiểm chứng đầu cho thấy
`chamsoc1` không có lịch nào để bấm "Ghi hồ sơ" — chỉ có lịch ngày mai và lịch đã xong. Thêm vào
`seed.py` một buổi đã qua nhưng chưa ghi hồ sơ. Đây đúng là câu hỏi luật mới bắt phải hỏi, và lần
này được hỏi **trước** khi đưa checklist.

## File đã thay đổi

**Ứng dụng**
- `app/models/care_record.py` (mới), `app/models/__init__.py`
- `app/services/care_records.py` (mới)
- `app/routers/care_records.py` (mới), `app/main.py`
- `app/templates/care_record_form.html`, `app/templates/pet_detail.html` (mới)
- `app/templates/appointments.html` — cột Thao tác mở cho `caretaker`, link ghi/xem hồ sơ
- `app/routers/appointments.py` — truyền `bay_gio` xuống template
- `app/seed.py` — 2 hồ sơ mẫu + 1 buổi đã qua chưa ghi hồ sơ

**Kiểm thử**
- `tests/unit/test_models_care_record.py` (mới, 5 test)
- `tests/unit/test_care_records_service.py` (mới, 21 test)
- `tests/integration/test_care_records.py` (mới, 11 test)

**Tài liệu**
- `docs/plans/2026-09-05-p4-ho-so-va-tiem-phong.md` (mới)
- `docs/testing/reports/2026-09-05-P4-chang1.md` (mới)
- `docs/user-stories.md`, `docs/erd.md`, `docs/testing/test-cases.md`,
  `docs/testing/smoke-checklist.md`, `docs/codebase-map.md`

## Kết quả test

```
$ pytest
329 passed in 30.91s

unit         238 passed in 13.10s   (ngân sách < 15s — đạt, trước 209/10,16s)
integration   91 passed in 18.57s   (ngân sách < 30s — đạt, trước 80)
```

**Ba vòng đỏ–xanh, mỗi vòng xác nhận đỏ trước:** `ModuleNotFoundError: app.models.care_record` →
`ImportError: cannot import name 'care_records'` → 9 test HTTP đỏ với 404.

**Năm thử nghiệm đột biến**, mỗi cái làm đỏ đúng test dự kiến: `staff_id` lấy từ người đăng nhập ·
`performed_at` lấy lúc bấm nút · bỏ chặn buổi chưa diễn ra · lịch sử sắp tăng dần · bỏ kiểm "lịch của
chính mình".

**Máy canh ranh giới bắt được chính tác giả của nó.** `test_architecture.py` dựng ở phiên này làm đỏ
suite ngay sau khi viết xong chặng 1: sáu file mới chưa vào `codebase-map.md`, và hai hàm public
(`lay_ho_so`, `ho_so_cua_lich`) chưa có test gọi thẳng. Cả hai đều là lỗi mà bốn phase trước phải nhờ
người dùng hoặc một đợt rà soát thủ công mới phát hiện.

Đối chiếu trong cùng một phiên: luật smoke checklist **bị phạm lần thứ tư**, còn hai luật đã thành
test thì **không lọt ca nào**. Đây là bằng chứng cụ thể nhất cho lập luận đã dùng khi chọn cơ chế.

**Kiểm chứng trên server thật:** buổi chưa ghi có nút "Ghi hồ sơ", buổi đã xong có "Xem hồ sơ", lịch
ngày mai không có nút nào; lưu tình trạng rỗng → 400; lưu hợp lệ → 303 và dòng chuyển "Hoàn thành";
sau đó nút Hủy biến mất và `POST .../huy` trả 400; lễ tân xem được hồ sơ nhưng `POST` trả 403.

## Còn dở / phiên sau làm gì

**Khối smoke "P4 chặng 1" (10 ô)** — tách sẵn theo chặng ngay từ đầu, có ghi chú chỉ rõ dữ liệu mẫu
đã dựng sẵn trạng thái nào để bấm thử.

**Chặng 2:** TC-059→064, TC-020, và sửa lỗi xóa thú cưng còn tham chiếu → 500 (đã tồn tại từ P3, test
tái hiện phải chạy đỏ trước).

Lưu ý: tầng unit 13,10s / ngân sách 15s. Chặng 2 thêm bảng `vaccinations` vào fixture `db`. Ngưỡng
xem lại 20s — nếu chạm thì chuyển fixture sang tạo schema một lần cho cả phiên rồi rollback.
