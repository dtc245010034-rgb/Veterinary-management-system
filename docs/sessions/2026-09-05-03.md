# Phien 2026-09-05-03

- **Ngay:** 2026-09-05
- **Muc tieu phien:** (1) Viết `tests/e2e/test_full_flow.py` — kéo TC-101 lên sớm từ P5, làm bước
  1 → 6 của kịch bản xuyên suốt. (2) **P4 chặng 2 — tiêm phòng**, đóng mốc KT2.

## Quyet dinh

1. **E2e chỉ được đi theo link và nút lấy từ HTML trả về; địa chỉ gõ tay duy nhất là `/`.**
   Đây là điều kiện tồn tại của file, không phải chi tiết cài đặt. Viết e2e theo kiểu cũ (tự dựng
   URL, tự dựng `data={}`) thì nó xanh mà vẫn để lọt đúng năm lỗi đã tìm ra ngày 05/09.
2. **Tự viết trình duyệt tí hon (`TrinhDuyet`) thay vì thêm phụ thuộc.** ~120 dòng trên
   `html.parser` của thư viện chuẩn. Điểm quan trọng nhất của nó: `<select>` không có option nào
   `selected` thì gửi **option đầu tiên**, đúng như trình duyệt — chính hành vi đó là nguyên nhân
   lỗi nặng nhất của ngày 05/09.
3. **Chạy trên CSDL file thật, mỗi request một session riêng** như production. Fixture `db` dùng
   chung một session cho cả test lẫn ứng dụng nên lỗi "quên commit" không lộ ra.
4. **Dữ liệu mẫu có hai nhân viên chăm sóc, người được phân lịch không đứng đầu danh sách.** Một
   nhân viên thì bước "đổi lịch giữ nguyên người thực hiện" xanh một cách tình cờ.
5. **TC-101 để 🟡 chứ không ✅** — mới 6/11 bước; bước 7→11 cần P5, P6, P7.
6. **Ô `checkbox`/`radio` ném `NotImplementedError` thay vì đoán.** Dự án chưa có ô nào như vậy;
   khi P5 thêm, người viết sẽ thấy lỗi ngay thay vì được một test xanh vì lý do sai.

## File da thay doi

| File | Việc |
|---|---|
| `tests/e2e/test_full_flow.py` | **Mới.** Trình duyệt tí hon + kịch bản TC-101 bước 1→6 |
| `docs/plans/2026-09-05-e2e-xuyen-suot.md` | **Mới.** Kế hoạch đã duyệt, 6/6 ô tick |
| `docs/testing/reports/2026-09-05-e2e-xuyen-suot.md` | **Mới.** Báo cáo + kết quả 4 đột biến |
| `docs/testing/test-cases.md` | TC-101 ⬜ → 🟡 kèm ghi chú phạm vi |
| `docs/testing/test-strategy.md` | Mục 6: đánh dấu 6/11 bước đã chạy được; thêm luật riêng của tầng e2e |
| `docs/codebase-map.md` | `tests/e2e/test_full_flow.py` chuyển sang mục "Hiện có" |

Không sửa dòng code ứng dụng nào. Bốn đột biến đều đã hoàn nguyên, kiểm chứng bằng lần chạy xanh
cuối cùng.

## Ket qua test

```
pytest                     345 passed in 31.30s
pytest tests/unit          240 passed in 11.82s   (ngan sach 15s)
pytest tests/integration   104 passed in 20.01s   (ngan sach 30s)
pytest tests/e2e             1 passed in  4.34s   (ngan sach: vai phut)
```

**Chứng minh test bắt được lỗi — 4 đột biến, tất cả đều đỏ đúng chỗ:**

| Đột biến | Assert đỏ |
|---|---|
| Bỏ `selected` ở ô chọn nhân viên trong form đổi lịch | `"Miu" in van_ban` ở *Lịch của tôi* |
| Tắt kiểm tra trùng lịch | `ma == 400` khi đặt lịch chèn giờ |
| `performed_at` lấy `clock.now()` | ngày giờ trong lịch sử thú cưng |
| `db.commit()` → `db.flush()` khi tạo chủ nuôi | tên chủ nuôi không hiện sau khi thêm |

**Con số đáng nhớ:** chạy riêng đột biến thứ nhất trên `tests/unit` + `tests/integration` →
**344/344 vẫn xanh, exit 0.** Chỉ e2e đỏ. Tầng e2e không trùng lặp ba tầng kia.

## Con do sau phan 1 — DA LAM XONG NGAY TRONG PHIEN NAY (xem phần 2 bên dưới)

**Việc kế tiếp: P4 chặng 2 — tiêm phòng.** Kế hoạch đã có sẵn trong
`docs/plans/2026-09-05-p4-ho-so-va-tiem-phong.md`, bước 0 (sửa mâu thuẫn US-18/TC-062) đã làm ở
chặng 1. Còn: model `vaccinations`, `app/services/vaccinations.py`, trang `/vaccinations` danh sách
đến hạn, khối hồ sơ tiêm trên `/pets/{id}` để đóng nốt TC-020. TC-059 → TC-064. Xong chặng này là
**hết mốc KT2**.

Ba điểm nhỏ vẫn chưa sửa, chờ người dùng quyết:

1. Thông báo lỗi form chủ nuôi hiện ở cột trái, xa ô nhập ở cột phải — sửa đúng phải đổi bố cục.
2. Dữ liệu mẫu chỉ cho mỗi thú cưng một hồ sơ nên ô smoke "lịch sử mới nhất lên đầu" không kiểm
   được bằng mắt.
3. Trang lịch hẹn tràn ngang ở bề rộng ~950px; chữ "Ghi hồ sơ" xuống hai dòng tạo vùng chết.

Lưu ý cho phiên sau:

- Khi thêm form mới có `<select>`, nhớ kịch bản e2e chọn theo **nhãn** — đổi nhãn hiển thị là e2e
  đỏ. Đó là ý đồ: nhãn là thứ người dùng nhìn thấy.
- E2e có hai nhân viên chăm sóc trong dữ liệu mẫu; đừng rút xuống một để "cho gọn".
- `test_architecture.py` đỏ thì siết code cho khớp, đừng xóa phép kiểm. CLAUDE.md mục 9 đang 3/5 dòng.


---

# Phần 2 — P4 chặng 2: tiêm phòng

## Quyet dinh

7. **Sửa spec US-18 lần thứ hai, trước khi viết code.** Danh sách đến hạn **chỉ tính mũi mới nhất
   của mỗi loại vắc-xin trên mỗi thú cưng**. Lấy đúng từng chữ bản cũ thì mũi 1 có hạn nhắc đã qua
   sẽ nằm lì trong danh sách quá hạn vĩnh viễn dù mũi 2 đã tiêm — lời nhắc đã hoàn thành mà không
   bao giờ tắt được thì cả màn hình mất tác dụng.
8. **Tách `app/routers/pets.py`.** Trang `/pets/{id}` nằm nhờ trong `care_records.py` từ chặng 1;
   nay nó gộp hai miền nghiệp vụ nên về đúng chỗ đã ghi trong `architecture.md` từ KT1.
9. **Không có phép kiểm vai trò nào trong tiêm phòng** — US-02 cho cả ba vai trò toàn quyền. Ghi rõ
   trong docstring để lần sau không ai tưởng là quên.
10. **Ô ngày tiêm có `max` là hôm nay.** Trình duyệt chặn trước, tầng services vẫn kiểm lại.
11. **Bỏ một ô khỏi smoke, có ghi lý do.** Trạng thái rỗng của danh sách đến hạn không dựng được
    bằng giao diện vì không có chức năng xóa mũi tiêm. Ghi rõ thay vì đưa ô không tick thật được.

## File da thay doi (phần 2)

| File | Việc |
|---|---|
| `app/models/vaccination.py` | **Mới.** Bảng `vaccinations`, property `qua_han` |
| `app/services/vaccinations.py` | **Mới.** `ghi_mui_tiem`, `ho_so_tiem`, `den_han` |
| `app/routers/vaccinations.py` | **Mới.** `/vaccinations`, `/pets/{id}/vaccinations` |
| `app/routers/pets.py` | **Mới.** `/pets/{id}` chuyển sang từ `care_records.py` |
| `app/templates/vaccinations.html` | **Mới.** Danh sách đến hạn |
| `app/templates/pet_detail.html` | Thêm khối hồ sơ tiêm + form ghi mũi |
| `app/templates/base.html`, `home.html` | Link **Tiêm phòng** cho cả ba vai trò |
| `app/routers/care_records.py`, `app/main.py`, `app/models/__init__.py` | Đăng ký router/model mới |
| `app/seed.py` | 5 mũi tiêm mẫu — dựng sẵn bốn trạng thái cần nhìn khi smoke |
| `tests/unit/test_models_vaccination.py` | **Mới**, 9 ca |
| `tests/unit/test_vaccinations_service.py` | **Mới**, 18 ca |
| `tests/integration/test_vaccinations.py` | **Mới**, 14 ca |
| `docs/user-stories.md`, `docs/testing/test-cases.md` | Sửa spec US-18/TC-062; TC-020 và TC-059→064 ✅ |
| `docs/testing/smoke-checklist.md` | Viết lại khối P4 chặng 2 theo dữ liệu mẫu thật |
| `docs/codebase-map.md`, `docs/plans/2026-09-05-p4-*.md` | Cập nhật |
| `docs/testing/reports/2026-09-05-P4-chang2.md` | **Mới.** Báo cáo chặng 2 |

## Ket qua test (cuối phiên)

```
pytest                     391 passed in 40.03s
pytest tests/unit          272 passed in 15.25s   (ngan sach 15s — hoi qua, nguong xem lai 20s)
pytest tests/integration   118 passed in 24.87s   (ngan sach 30s)
pytest tests/e2e             1 passed in  5.02s
```

**7 đột biến cho phần tiêm phòng, tất cả đỏ đúng chỗ**, hoàn nguyên xong xanh lại: bỏ luật "chỉ tính
mũi mới nhất", ranh giới `<=` → `<`, chặn nhầm ngày tiêm hôm nay, chặn nhầm hạn nhắc bằng ngày tiêm,
coi hạn đúng hôm nay là quá hạn, bỏ link menu, bỏ dòng khuyến cáo bác sĩ thú y.

Một test xanh ngay lần đầu vì lý do sai (`test_han_con_xa_khong_hien_trong_danh_sach` chỉ kiểm "tên
không xuất hiện", mà trang đang 404) — đã thêm `assert r.status_code == 200`.

`test_architecture.py` đỏ một lần: 6 file mới chưa có trong `codebase-map.md`.

**Ma trận: 60 ✅ · 4 🟡 · 38 ⬜** (từ 53 ✅ đầu phiên).

## Con do / phien sau lam gi

**Mốc KT2 đã xong phần code. Còn đúng một việc: người dùng smoke test khối P4 chặng 2** trên trình
duyệt thật (`python -m app.seed` đã chạy, CSDL sạch). Hai ô cuối trong Definition of Done của
`docs/plans/2026-09-05-p4-ho-so-va-tiem-phong.md` chờ đúng việc đó.

Sau đó là **P5 — hóa đơn và thanh toán** (US-19 → US-21, TC-065 → TC-075), và nối bước 7→9 của e2e.

Lưu ý cho phiên sau:

- Tầng unit 15,25s, đã hơi quá ngân sách 15s. Qua 20s ở P5 thì phải xem lại thật.
- E2e chọn trong `<select>` theo **nhãn**; đổi nhãn hiển thị là e2e đỏ. Đó là ý đồ.
- P5 sẽ là form đầu tiên có thể cần ô `checkbox` — `_BocTrang` trong e2e sẽ ném
  `NotImplementedError` chứ không đoán. Bổ sung nó khi tới đó.
- `test_architecture.py` đỏ thì siết code cho khớp, đừng xóa phép kiểm. CLAUDE.md mục 9 đang 3/5 dòng.


---

# Phần 3 — Rà luồng bằng trình duyệt (ngày 06/09, cùng phiên)

Người dùng yêu cầu rà luồng bằng Claude in Chrome trước khi họ smoke test. Trong lúc rà, họ đã tick
xong 10/10 ô của khối P4 chặng 2.

## Nam loi tim duoc, tat ca da sua

| # | Lỗi | Mức |
|---|---|---|
| 1 | Xóa thú cưng chỉ có hồ sơ tiêm → trang đen "Internal Server Error" | **nặng** |
| 2 | `caretaker` có quyền xem chủ nuôi (US-02) nhưng không có link menu | vừa |
| 3 | Ba class CSS không tồn tại: `chinh-nhe`, `bat-buoc`, `dong-canh-bao` | nhẹ |
| 4 | Nhãn "Quá hạn" dùng chung kiểu xám với "Đã hủy" — làm mờ đúng thứ cần nổi | nhẹ |
| 5 | Form ghi mũi tiêm xóa sạch năm ô đã nhập khi báo lỗi | vừa |

Chi tiết: [`../testing/reports/2026-09-06-ra-luong-P4-chang2.md`](../testing/reports/2026-09-06-ra-luong-P4-chang2.md).

## Quyet dinh

12. **`xoa_thu_cung` để khóa ngoại quyết định thay vì liệt kê bảng.** Bắt `IntegrityError` →
    `rollback` → `LoiNghiepVu`. Chặn sẵn mọi bảng thêm ở P5–P7. Đổi lại thông báo không nói được
    chính xác loại dữ liệu nào đang giữ. Code ngắn hơn trước.
13. **Bài học lặp 3 lần và tự động hóa được thì thành test**, không thành dòng ghi nhớ:
    `test_moi_class_dung_trong_template_deu_co_trong_css`.
14. **Bài học lặp 2 lần và không tự động hóa được thì thành dòng 4/5 của CLAUDE.md mục 9:**
    *sửa một lỗi thì soát cả lớp lỗi, không chỉ ca vừa thấy.*

## Ket qua test

```
pytest   398 passed in 41.70s   (tang 7 so voi truoc dot ra)
```

Phép canh CSS ban đầu **bỏ sót đúng class đã gây lỗi** (`dong-canh-bao` nằm trong biểu thức Jinja).
Siết lại rồi mới bắt đủ ba. Đột biến bỏ `try/except IntegrityError` → 4 test đỏ, hoàn nguyên xanh lại.

## Con do

Năm ô smoke mới trong khối **"Sau đợt sửa lỗi ngày 06/09"** chờ người dùng tick. CSDL đã seed lại
sạch, `petcare.db` ở trạng thái tài liệu mô tả.

**Cảnh báo cho P5:** `xoa_chu_nuoi` vẫn dùng cách liệt kê bảng như `xoa_thu_cung` trước khi sửa.
Hiện đúng vì mới chỉ có `pets` trỏ vào `owners`. P5 thêm `invoices` trỏ vào `owners` là lỗi 500
tương tự sẽ xuất hiện — sửa cùng lúc với việc thêm bảng.

Ba điểm nhỏ khác vẫn chưa sửa: form chủ nuôi và form thú cưng cũng mất dữ liệu khi báo lỗi; thông
báo lỗi form chủ nuôi đặt xa ô nhập; trang lịch hẹn tràn ngang.


## Phan 3b — Sua not cac diem nho ton dong (cung ngay 06/09)

Người dùng yêu cầu dọn hết danh sách "chưa sửa" trước khi sang P5. Sáu việc, xong cả sáu:

| Việc | Cách sửa |
|---|---|
| Form chủ nuôi và form thú cưng mất dữ liệu khi báo lỗi | Trả lại giá trị đã nhập |
| Thông báo lỗi form chủ nuôi nằm xa ô nhập | Tách `loi` (cả trang) và `loi_nhap` (trong khung nhập) |
| Lưới lịch hẹn tràn ngang ở ~1030px | `.hai-cot.cot-rong` xếp dọc từ 1100px |
| Link "Ghi hồ sơ" xuống hai dòng có vùng chết | `white-space: nowrap` |
| Mỗi thú cưng chỉ một hồ sơ chăm sóc | Mực có thêm buổi cách gần một tháng |
| `xoa_chu_nuoi` sẽ lặp lỗi 500 ở P5 | Thêm `IntegrityError` làm lớp chặn cuối |

Bốn test tái hiện đều **đỏ trước**. **402 passed.** Đã kiểm lại bằng mắt trên trình duyệt: lưới lịch
hẹn không còn tràn ngang ở 1060px, lỗi form chủ nuôi hiện trong khung nhập kèm dữ liệu cũ, trang Mực
có hai hồ sơ mới nhất lên đầu.

Suýt hỏng một chỗ: dời khối `loi` vào trong form làm lỗi "xóa chủ nuôi còn thú cưng" cũng rơi vào
khung "Thêm chủ nuôi". Phát hiện khi đọc lại, tách thành hai biến.

**Còn lại, có chủ ý không sửa:** thanh điều hướng vỡ ở cửa sổ hẹp ~800px (dự án nhắm màn hình để
bàn); `owner_detail.html` vẫn để thông báo lỗi ở đầu trang vì trang đó có hai nguồn lỗi khác nhau.

Mười ô smoke đang chờ người dùng tick: 5 ô khối "Sau đợt sửa lỗi 06/09" + 5 ô khối "Sửa nốt các
điểm nhỏ tồn đọng".
